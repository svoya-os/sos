# SPDX-License-Identifier: Apache-2.0
id = "voice"
name = { en = "Voice (speech ↔ text)", ru = "Голос (речь ↔ текст)" }
summary = { en = "Local speech recognition (Parakeet TDT v3: Russian, English, Estonian) and speech synthesis (Chatterbox, Qwen3-TTS) for Jackson's push-to-talk.", ru = "Локальное распознавание речи (Parakeet TDT v3: русский, английский, эстонский) и синтез (Chatterbox, Qwen3-TTS) для push-to-talk Джексона." }
category = "media"
profiles = ["creator"]
requires = ["base-ai"]
apt = ["ffmpeg", "sox", "libsndfile1"]
flatpak = []
scripts = { install = "install.sh", remove = "remove.sh" }
disk_gb = 3.5
vram_gb_min = 0
aliases = ["speech", "tts", "stt", "голос"]
license_note = { en = "Parakeet TDT v3: CC-BY-4.0 (attribution); Chatterbox: MIT; Qwen3-TTS: Apache-2.0; sherpa-onnx: Apache-2.0. Recognition runs on the CPU too.", ru = "Parakeet TDT v3 — CC-BY-4.0 (атрибуция); Chatterbox — MIT; Qwen3-TTS — Apache-2.0; sherpa-onnx — Apache-2.0. Распознавание работает и на процессоре." }
