# SOS: NetworkManager manages every network device, wired Ethernet included.
# This file shadows Ubuntu's /usr/lib/NetworkManager/conf.d/10-globally-managed-devices.conf, which
# leaves everything but Wi-Fi and mobile broadband unmanaged (a computer on a cable stayed offline).
# /etc/netplan/01-network-manager-all.yaml says the same the Ubuntu Desktop way.
