# Only for Python libraries the program uses: `uv add torch` (for `import nn`), `uv add numpy`…
# UpsiL itself comes with SOS (package upsil); `sos run main.upl` runs in .venv once it exists.
[project]
name = "{{ project.name }}"
version = "0.1.0"
description = "{{ project.description }}"
requires-python = ">=3.10"
dependencies = []
{{ torch_sources }}
