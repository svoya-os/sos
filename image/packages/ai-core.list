# SOS AI core: only what every profile needs. Everything else is a module (`sos modules add ...`):
# llama.cpp (+ libggml0-backend-vulkan), ROCm, CUDA (multiverse, installed later — never on the ISO),
# NVIDIA Container Toolkit, ComfyUI, ... SPDX-License-Identifier: Apache-2.0
python3
python3-venv
python3-pip
uv
git
git-lfs
jq
sqlite3
podman
# `docker …` runs Podman (the same commands; /etc/containers/nodocker, from svoya-base, hides its notice)
?podman-docker
uidmap
passt
?fuse-overlayfs
vulkan-tools
?clinfo
?nvtop
