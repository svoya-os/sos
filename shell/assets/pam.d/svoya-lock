#%PAM-1.0
# SOS lock screen (shell/lock/Lock.qml). Installed to /etc/pam.d/svoya-lock by the
# svoya-shell package. Password only; add fingerprint etc. here if wanted, e.g.
#   auth sufficient pam_fprintd.so max-tries=1
auth    required    pam_unix.so
