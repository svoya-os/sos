import QtQuick
import qs.core

// Mono uppercase group caption (ПРИЛОЖЕНИЯ, НАСТРОЙКИ…): +0.14em tracking, textFaint.
MText {
    caps: true
    size: 10.5
    color: Theme.textFaint
    font.weight: Font.Medium
}
