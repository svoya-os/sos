# SOS layout preset «Классика» / Classic.  SPDX-License-Identifier: Apache-2.0
# Assumed Hyprland: 0.53.3 (hyprlang), valid up to 0.56.x — see ../hyprland.conf.
#
# Windows behave like Clean (floating, title bars, Super+T tiles one workspace); the shell draws
# the bar at the bottom with a taskbar. Sourced when svoya-shell.conf says `$svoya_preset = classic`.

source = ./clean.conf
