import QtQuick
import qs.core
import qs.components

// The bar's type: Plex Mono 500 11.5px, +0.02em, textDim.
MText {
    size: Theme.fsSystem
    font.weight: Font.Medium
    font.letterSpacing: 0.02 * Theme.fsSystem
    color: Theme.textDim
}
